steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: bash
  args:
    - -c
    - |
      ls
      gsutil cp storage/flutter-survey-metadata.json gs://khanhs-test-bucket
options:
  logging: CLOUD_LOGGING_ONLY
