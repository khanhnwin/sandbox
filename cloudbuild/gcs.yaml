steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: bash
  args:
    - -c
    - |
      ls
      gsutil cp -r storage/flutter-survey-metadata.json gs://khanh-sandbox-bucket
options:
  logging: CLOUD_LOGGING_ONLY
